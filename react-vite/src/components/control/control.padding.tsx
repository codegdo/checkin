import React from 'react';
import { ControlData } from './control.type';

interface ControlPaddingProps extends ControlData {};

export function ControlPadding(props: ControlPaddingProps) {
    return <div>PADDING</div>
}