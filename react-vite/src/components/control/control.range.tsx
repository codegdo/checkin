import React from 'react';
import { ControlData } from './control.type';

interface ControlRangeProps extends ControlData {};
export function ControlRange(props:ControlRangeProps) {
    return <div>RANGE</div>
}